
// bell.sidebar

{
	init() {
		
	},
	dispatch(event) {
		let APP = bell,
			Self = APP.sidebar,
			el;
		// console.log(event);
		switch (event.type) {
			// custom events
			case "some-event":
				break;
		}
	}
}
