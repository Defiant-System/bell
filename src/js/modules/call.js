
// bell.call

{
	init() {
		
	},
	dispatch(event) {
		let APP = bell,
			Self = APP.call,
			el;
		// console.log(event);
		switch (event.type) {
			// custom events
			case "some-event":
				break;
		}
	}
}
